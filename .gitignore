node_modules
.next
out
.env
*.log
server.pid
.DS_Store
wp-extract
wp.zip
.vercel
.env*.local
# `vercel env pull` écrit ici : le fichier contient le jeton GitHub, la clé de
# service Supabase et le jeton Vercel. Il ne doit jamais partir dans le dépôt.
.env.vercel
.env.local.sauvegarde
tiktok-bot/latest-recipe.txt

.claude/
graphify-out/
wp-writeback-ids.txt

# Photos générées : les vignettes carte partent en production (servies en statique
# par Vercel, référencées dans mockData). Les grandes (*.webp sans -carte) restent
# locales — sources non utilisées par le site.
/public/recipes-ia/*
!/public/recipes-ia/*-carte.webp
