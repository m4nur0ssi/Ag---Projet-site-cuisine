node_modules
.next
out
.env
*.log
server.pid
.DS_Store
wp-extract
wp.zip
.vercel
.env*.local
# `vercel env pull` écrit ici : le fichier contient le jeton GitHub, la clé de
# service Supabase et le jeton Vercel. Il ne doit jamais partir dans le dépôt.
.env.vercel
.env.local.sauvegarde
tiktok-bot/latest-recipe.txt

.claude/
graphify-out/
wp-writeback-ids.txt

# Photos générées : cartes (760px, mockData) ET grandes (1200px, HD dans la fiche
# via grandePhoto()) partent en production, servies en statique par Vercel.
# Seuls les éventuels autres fichiers (png sources, etc.) restent locaux.
/public/recipes-ia/*
!/public/recipes-ia/*.webp
scripts/.releve-prix.json
